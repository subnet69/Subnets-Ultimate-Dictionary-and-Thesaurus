[pytest]
# Ensure pytest emits JUnit XML in xunit1 format so test file paths are included
addopts = --junit-xml=./junit.xml -o junit_family=xunit1

# Optional: set a suite name if you want
# junit_suite_name = my_suite
